// 배열 디스트럭처링 2

let x, y, z;
[x, y, z] = [1, 2, 3];
console.log(x,y,z)  // 1 2 3

let [x2, y2, z2] = [1, 2, 3];
console.log(x2,y2,z2)   // 1 2 3

// 배열 디스트럭처링을 위해서는 할당 연산자의 왼쪽에 배열 형태의 변수 리스트가 필요하며
// 오른쪽에는 배열형태의 요소(값) 이 있어야한다.
// 이때 추출/할당 기중은 배열의 인덱스이다.


